spring.application.name=arthasetu-backend

# Server
server.port=8080

# MySQL Database
spring.datasource.url=jdbc:mysql://localhost:3306/arthasetu_db
spring.datasource.username=root
spring.datasource.password=Sri@6766

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JPA
spring.jpa.open-in-view=false

# SQL initialization
spring.jpa.hibernate.ddl-auto=update

